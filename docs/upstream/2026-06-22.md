# Upstream Port Manifest - 2026-06-22

Policy: ADR 0009 content-ahead-only. Upstream changes are ported or rewritten
against the local adapter/cache/aggregation architecture; upstream content is
not merged.

## Parser Correctness Port

```
upstream PR / SHA: #735 / 783bbb8d
decision: port
local commit: pending
cache impact: source-message cache v27
tests: sessions::codex::tests::test_forked_child_same_millisecond_turn_starts_own_session
reason: compare only the UUIDv7 48-bit millisecond prefix so same-ms fork child
        turns keep their own token row.

upstream PR / SHA: #737 / 686f3cf2; #766 / 235230ac
decision: port
local commit: pending
cache impact: source-message cache v27
tests: sessions::antigravity_cli::tests
reason: Antigravity CLI rows use per-generation protobuf timestamps; untrusted
        varint token counts clamp/saturate instead of wrapping.

upstream PR / SHA: #741 / b49cec19
decision: rewrite
local commit: pending
cache impact: source-message cache v27; source digest paths include siblings
tests: message_cache::tests::fingerprint_with_sibling_invalidates_on_sibling_only_change;
       adapters::vscode_tasks::tests
reason: generic FingerprintPolicy::PrimaryWithSiblings covers Roo/Kilo/Cline
        ui_messages.json plus api_conversation_history.json.

upstream PR / SHA: #750 / db88138b
decision: port
local commit: pending
cache impact: source-message cache v27
tests: sessions::commandcode::tests
reason: Command Code canonicalizes model ids without unsafe UTF-8 slicing,
        recovers provider hints for pricing, and uses per-turn input deltas.

upstream PR / SHA: #752 / 24e3771c
decision: port
local commit: pending
cache impact: source-message cache v27
tests: sessions::kiro::tests
reason: Kiro globalStorage traversal deduplicates structurally equal alias
        subtrees while keeping distinct subtrees.

upstream PR / SHA: #760 / 5ff7bf44
decision: port session subset
local commit: pending
cache impact: source-message cache v27
tests: tests::test_finalize_token_priced_messages_canonicalizes_provider;
       sessions::pi::tests::test_parse_pi_keeps_missing_provider_with_model_inference;
       sessions::droid::tests::test_parse_droid_file_keeps_usage_when_timestamp_missing;
       sessions::qwen::tests; sessions::mux::tests; sessions::zed::tests
reason: provider canonicalization lives in finalize_token_priced_messages so
        fresh parse and cache hits share the same semantics; valid token rows
        are not dropped just because provider/timestamp metadata is incomplete.
```

## Deferred Or Aborted

```
#723/#724/#751 Copilot: rewrite complete with local semantics. Supports
dot/underscore cache token attributes. Agent display uses gen_ai.agent.name
when present; blank or missing names become "Default". gen_ai.agent.id is not
used for Plugin/team special-casing or guessed display labels. Source-message
cache v27 and TUI cache v18.

#727/#749 Junie: rewrite complete as a LocalSourceAdapter. Added catalog entry,
local scan definition, parser, adapter, source-message cache tests, and frontend
registry generation. modelUsage.cost is ignored and cost-only rows are dropped.

#728/#759 usage providers: port/rewrite complete for explicit provider errors.
UsageFetchBatch surfaces provider errors to text/TUI and structured JSON stderr.
MiniMax Token Plan currently follows upstream environment-variable credential
design by explicit local decision.

#757 Codex reset credits: defer until this branch defines a reset-credit output
surface; do not copy dashboard-only UsageOutput fields ahead of that design.

#760 usage subset: port complete for Amp empty metrics/Unicode slicing,
Copilot/MiniMax empty metrics, Z.ai percentage, Kimi reset-window dedup, and
MiniMax saturating time conversion.

#744 router/orchestrator pricing: abort for this personal branch. This is not
worth carrying until real local usage and billing data justify a branch-native
design.

#731/#748 dashboard: defer; current usage TUI architecture is not the upstream
dashboard.

#745/#756/#765 subscription HTML scraper: abort for this personal branch as an
unused vendor surface backed by unstable cookie-authenticated HTML scraping.

#720/#721/#726/#746/#753/#755 report and Apple-FM: abort for this round; no
local report surface.

#742/#747 MiCode/MiMo Code: abort. The local MiMo Code client was removed under
ADR 0012, so the cross-DB dedup and seconds/milliseconds fixes have no target
surface. The #742 embedded vendor-cost hunk remains permanently rejected under
ADR 0011.

#743 GJC header-less collision: abort. The local GJC/gajae-code client was
removed under ADR 0012, so the header-less session namespace fix has no target
surface.

#754 Jcode: abort for this round; no local Jcode adapter/catalog entry.

#465 legacy flags: already covered/no-op.

#763/#764/#762 provider display/Fable: port complete for core Fable provider
inference; TUI provider display already split merged provider lists through the
shared display mapper.

#767/#768 docs/domain: port partial complete. Legacy custom pricing schema and
settings URLs were changed to tokscale.ai. README support docs were also aligned
with this branch by removing excluded local clients and by documenting Warp/Oz
only as a subscription usage cache surface, not a local token report source. No
broad upstream README sync, report/dashboard docs, workflow, or badge changes.
```
