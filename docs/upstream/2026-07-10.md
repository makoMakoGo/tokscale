# Upstream Port Manifest - 2026-07-10

Policy: ADR 0009 content-ahead-only. Upstream changes are ported or rewritten
against the local adapter/cache/aggregation architecture; upstream content is
not merged.

## Ported Or Rewritten

```
upstream SHA: 59421da9; 3527f8ff
decision: rewrite
local commit: pending
cache impact: Antigravity CLI, Junie, and Zed parser revisions incremented
tests: overflow-focused core, aggregation, sessionize, graph, and TUI tests
reason: use checked token arithmetic and explicit failures under ADR 0001.
        Do not silently clamp corrupt usage to i64::MAX/u64::MAX.

upstream SHA: 31deb7e6
decision: port
local commit: pending
cache impact: Mux parser revision incremented
tests: sessions::mux::tests::dedup_key_is_workspace_scoped_and_stable_when_models_are_added
reason: derive Mux dedup identity from workspace/session plus the raw model key,
        not a positional model index.

upstream SHA: 65f8f3e2
decision: partial rewrite for the local JSONL adapter
local commit: pending
cache impact: ZCode parser revision incremented
tests: sessions::zcode::tests
reason: accept camelCase usage fields and use reported totals to distinguish
        cache/reasoning-inclusive input/output from exclusive buckets. Preserve
        the established prompt-token behavior when no total is available. The
        fork has no ZCode SQLite source, so the upstream SQLite path is omitted.

upstream SHA: 4cbc2f6b
decision: port
local commit: pending
cache impact: Claude parser revision incremented
tests: workflow_journal_is_never_ingested;
       deep_workflow_transcript_resolves_parent_agent_and_counts_tokens;
       claude_adapter_discovers_default_transcripts_extra_and_cc_mirror_roots
reason: exclude workflow journals and resolve parent sessions for deeply nested
        subagent workflow transcripts.

upstream SHA: 85669602
decision: documentation-only port; behavior already covered locally
local commit: pending
cache impact: none
tests: existing Codex adapter discovery and cross-root dedup tests
reason: document that the local Codex adapter scans sessions and
        archived_sessions with content-derived dedup identity.

upstream SHA: 44db59df; b4ff8a73; 4258514a
decision: port and adapt to the local responsive TUI
local commit: pending
cache impact: none
tests: TUI theme round-trip, surface colors, saved-theme precedence, explicit
       invalid-theme error, and theme cycling
reason: add Graphite, Lagoon, and Dusk; honor saved colorPalette when --theme
        is absent; keep explicit CLI precedence; share bold total-token cells
        across Models, Daily, Period, Hourly, and Agents tables.
```

## Already Covered

```
2c781b63 monthly TUI: the fork already has monthly and weekly period views.
b8156e64 Pi/OMP title handling: OMP is a separate local adapter with its own
         accepted session semantics.
1cae006d reset-credit test: no local reset-credit output surface exists.
da5e06d2 negative zero: aggregate report totals already normalize -0.0.
```

## Deferred Or Rejected

```
88b32ac8; 23cf62e0 Copilot agent id: reject. The fork intentionally uses
         gen_ai.agent.name and does not infer display identity from agent.id.

09344531; 405ded4a; 315549b4 Kiro IDE parsers: defer by maintainer decision;
         the Kiro IDE surface is not used. Existing Kiro CLI support is unchanged.

dcb053e0 JCode: reject under ADR 0012; the client was removed locally.

49c7a55c; 0bef0763; d3107970 hosted frontend/badge/settings: reject under the
         local frontend removal documented by ADR 0015.

6c804711 generic pricing prefix fallback: reject under ADR 0013. Unresolved raw
         model ids keep usage and receive derived cost 0.0 without substitution.

cd394af2 Crush registry: defer; there is no accepted local token source to
         justify adding a client registry entry.

0f84d174 OpenCode provider costs: reject under ADR 0011. Local costs are derived
         from token buckets and the accepted pricing authority, not vendor rows.

555be539 hide zero-token rows: reject as a presentation fallback. Non-usage rows
         are removed at the positive-token accounting boundary.
```

## Version Commits

```
5bac6e8b 4.0.11
f4f93e4f 4.1.0
a0a5b0d1 4.1.1
415a1967 4.2.0
6f62a4de 4.3.0

decision: collapse upstream version-only commits. This port PR does not edit
package versions. A separately approved fork release may dispatch the publish
workflow with version 4.3.0 after the content PR and ancestry record land.
```

## Ancestry Record

After the content PR lands, record `origin/main` ancestry with the ADR 0009
`-s ours --no-ff` merge. Verify `git diff HEAD^1 HEAD` is empty before pushing.
